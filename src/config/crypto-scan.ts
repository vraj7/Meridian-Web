/** Target number of coins in the Delta-aligned crypto universe. */
export const CRYPTO_UNIVERSE_SIZE = 100;

/** Coins scanned per page / “Scan next” batch. */
export const CRYPTO_SCAN_BATCH_SIZE = 25;
